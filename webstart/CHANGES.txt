Changes for webstart prototype:

1. Change HelpFrame for load translations from classpath if translation doesn't exist on disk 
2. Add LFileCopy.copy for reader/writer
3. Change EditorController for display html from classpath instead /docs dir
4. Change build.xml
5. Change release/WebStart-specific/build.xml

TODO:
1. Change logger for webstart initialization
2. Add docs.jar into Netbeans' build
